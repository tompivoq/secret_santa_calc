node_modules
dist
# Everything under data/ is runtime-generated (the SQLite db and its
# -shm/-wal files, and the auto-generated auth secret — see
# auth/secret.ts) — none of it belongs in source control.
data/
*.tsbuildinfo
