# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code local overrides
.claude/settings.local.json

# Local database snapshots from `npm run db:backup`
backups

# Secrets — the Resend API key and friends live in the environment, never
# in the repo (see the env var table in README.md). Ignored so a stray
# local .env can't be committed by accident.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
